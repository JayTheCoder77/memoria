services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: memoria-postgres
    environment:
      POSTGRES_USER: memoria
      POSTGRES_PASSWORD: memoria
      POSTGRES_DB: memoria
    ports:
      - "5432:5432"
    volumes:
      - memoria-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U memoria -d memoria"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  memoria-pgdata:
